node_modules
# Keep environment variables out of version control
.env
.env.*
!.env.example

/src/generated/prisma

# Local Netlify folder
.netlify

# Next.js build output
.next
out

# Pinned local Node runtime
.runtime

# Local SQLite databases (may contain customer records)
*.db
*.db-journal
prisma/dev.db

# Build/tooling caches
tsconfig.tsbuildinfo
*.tsbuildinfo

# Dev screenshots. screenshot.js regenerates these on demand; committing them
# put ~29MB of build-irrelevant PNGs into every clone and CDN upload.
/public/images/uploads/screenshot_*.png
/public/images/uploads/crm-*.png
/Screenshot *.png

# macOS
.DS_Store
